[Core]
versions = ["lts", "1", "pre"]

# Core 32-bit dropped: SBML precompile hits ArgumentError (NULL C string) on
# i686 via SBML_jll (LCSB-BioCore/SBML.jl#282). Re-enable after that release.
# ["Core 32-bit"]
# group = "Core"
# versions = ["1"]
# os = ["ubuntu-latest"]
# arch = "x86"

[QA]
versions = ["lts", "1"]
